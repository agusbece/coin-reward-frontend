import { FC, memo } from 'react'

// eslint-disable-next-line react/display-name
const Test: FC = memo(() => {
    return (
        <>
            <h1>DAAA test bro</h1>
        </>
    )
})

export default Test
